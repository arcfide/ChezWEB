#! /usr/bin/env scheme-script
(import (chezscheme))

(with-exception-handler
  default-exception-handler
  (lambda ()
    (new-cafe 
      (lambda (x) 
        (eval x (environment '(chezscheme) '(arcfide chezweb tangle)))))))
